# Core scientific stack
numpy>=1.24.0
scipy>=1.10.0
matplotlib>=3.7.0
pandas>=2.0.0
PyYAML>=6.0.0

# Perception layer — DINOv2
# Install torch CPU-only to avoid CUDA dependency on local dev machine:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0
torchvision>=0.15.0
transformers>=4.35.0
accelerate>=0.24.0
Pillow>=9.0.0

# Clustering and metrics
scikit-learn>=1.3.0

# Dimensionality reduction for visualization
umap-learn>=0.5.0

# Experiment infrastructure
python-dotenv>=1.0.0
anthropic>=0.25.0

# Development
jupyter>=1.0.0
pytest>=7.0.0
